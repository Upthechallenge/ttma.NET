@model IEnumerable<domain.Entities.doctordisponibility>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_template.cshtml";
}
<section id="job">
    <div class="job-wrapper" >
        <div class="container">
            <div class="row">
                <div class="col-lg-6 col-md-6 col-sm-6">
                    <h2>
                        Check doctor disponbility</h2>


                        <table class="table">
                            <tr>
                                <th>
                                    @Html.DisplayNameFor(model => model.ID_disp)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.date_disp)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.doctor_name)
                                </th>
                                <th></th>
                            </tr>

                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.ID_disp)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.date_disp)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.doctor_name)
                                    </td>
                                    <td></td>
                                </tr>
                            }

                        </table>
</div></div></div></div></section>